<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Error</title>
  <script src="https://cdn.tailwindcss.com"></script>

  <style>
    /* Floating animation */
    @keyframes float {
      0% { transform: translateY(0px); }
      50% { transform: translateY(-10px); }
      100% { transform: translateY(0px); }
    }

    .float {
      animation: float 3s ease-in-out infinite;
    }
  </style>
</head>

<body class="bg-gradient-to-br from-gray-100 to-gray-300 min-h-screen flex items-center justify-center">

  <div class="bg-white p-10 rounded-2xl shadow-xl text-center max-w-md w-full">
    
    <!-- Animated Icon -->
    <div class="text-red-600 text-6xl mb-4 float">⚠️</div>

    <h1 class="text-3xl font-bold text-gray-800 mb-2">Oops! Something went wrong</h1>

    <p class="text-gray-600 text-lg mb-6">
      <%= typeof message !== "undefined" ? message : "Unexpected error occurred." %>
    </p>

    <a href="/"
       class="bg-blue-600 text-white px-6 py-3 rounded-lg shadow hover:bg-blue-700 transition duration-300 inline-block">
      ⟵ Go Back Home
    </a>

    <!-- Bottom Line -->
    <div class="mt-6 text-sm text-gray-500">
      If the problem continues, please contact support.
    </div>
  </div>

</body>
</html>
