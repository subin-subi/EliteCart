<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ELITEKART</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</head>
<body class="bg-gray-100">
  <!-- Navbar -->
  <div
    class="fixed z-50 top-0 w-screen h-16 sm:h-20 flex items-center justify-between px-8 bg-black border-b-2 border-yellow-400 shadow-lg">

    <!-- Left: Logo/Brand -->
    <div class="flex items-center space-x-2">
      <span class="text-3xl sm:text-4xl font-bold font-serif text-white tracking-wide">EliteKart</span>
    </div>

    <!-- Center: Nav Links -->
    <nav class="hidden md:flex items-center space-x-8 text-base sm:text-lg text-white font-medium">
      <a href="/" class="hover:text-yellow-400 transition">Home</a>
      <a href="/product" class="hover:text-yellow-400 transition">Shop</a>
      <a href="/contact" class="hover:text-yellow-400 transition">Contact us</a>
      <a href="/about" class="hover:text-yellow-400 transition">About</a>
    </nav>

    <!-- Right: Icons + Profile Dropdown -->
    <div class="flex items-center space-x-8">
   
      <!-- Icons -->
  <div class="hidden sm:flex items-center space-x-6 text-white text-xl">

    <!-- Chat Icon -->
<div class="relative">
  <a href="/chat" class="hover:text-yellow-400">
    <i class="fa-solid fa-comments text-xl"></i>
  </a>

  <span
    id="chatNotifBadge"
    class="hidden absolute -top-2 -right-2 bg-red-500 text-white text-xs rounded-full px-1.5 py-0.5"
  >
    0
  </span>
</div>


  <!-- Wishlist Icon -->
  <div class="relative">
    <a href="/wishlist" class="hover:text-yellow-400">
      <i class="fa-regular fa-heart"></i>
    </a>
    <span id="wishlistCount"
      class="hidden absolute -top-2 -right-3 bg-red-500 text-white text-xs font-semibold rounded-full w-5 h-5 flex items-center justify-center">
      0
    </span>
  </div>

  <!-- Cart Icon -->
  <div class="relative">
    <a href="/cart" class="hover:text-yellow-400">
      <i class="fa-solid fa-cart-shopping"></i>
    </a>
    <span id="cartCount"
      class="hidden absolute -top-2 -right-3 bg-red-500 text-white text-xs font-semibold rounded-full w-5 h-5 flex items-center justify-center">
      0
    </span>
  </div>

</div>

    <!-- HAMBURGER -->
    <button id="mobileMenuBtn" class="text-white text-2xl md:hidden">
      <i class="fa-solid fa-bars"></i>
    </button>


      <!-- Profile Dropdown -->
      <div class="relative">
      <button id="profileButton" class="relative hover:text-yellow-400 focus:outline-none flex items-center space-x-2">
  <img
    id="userProfileImage"
    src="/images/pf.jpg"
    alt="Profile"
    class="w-8 h-8 rounded-full border-2 border-yellow-400 object-cover"
  />
  <span id="userName" class="hidden text-white text-sm font-medium"></span>
</button>


        <!-- Dropdown Menu -->
        <div id="profileMenu"
          class="hidden absolute right-0 mt-3 w-56 bg-white border border-gray-200 rounded-lg shadow-lg z-50">

          <!-- When not logged in -->
          <div id="guestMenu" class="p-4 border-b">
            <span class="text-sm text-gray-600">Welcome!</span>
            <div class="mt-2 flex gap-2">
              <a href="/login"
                class="text-sm font-semibold text-gray-900 bg-gray-100 px-3 py-1 rounded hover:bg-gray-200">Login</a>
              <a href="/signup"
                class="text-sm font-semibold text-white bg-yellow-600 px-3 py-1 rounded hover:bg-yellow-700">Sign
                Up</a>
            </div>
          </div>

          <!-- When logged in -->
          <ul id="userMenu" class="hidden py-2 text-sm text-gray-700">
            <li>
              <a href="/profile" class="flex items-center px-4 py-2 hover:bg-gray-100">
                <i class="fa-regular fa-user mr-2"></i> My Profile
              </a>
            </li>
            
          
            <li>
              <button id="logoutButton"
                class="w-full text-left flex items-center px-4 py-2 hover:bg-gray-100">
                <i class="fa-solid fa-right-from-bracket mr-2"></i> Logout
              </button>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>


<!-- MOBILE MENU -->
<div id="mobileMenu"
     class="hidden fixed top-16 left-0 w-full z-50 md:hidden
            transition-all duration-300 ease-out
            opacity-0 -translate-y-6 scale-95">

  <nav class="flex flex-col items-center space-y-6 py-6 text-lg font-semibold">

    <a href="/" 
       class="text-black hover:text-orange-500 transition">
      Home
    </a>

    <a href="/product"
       class="text-black hover:text-orange-500 transition">
      Shop
    </a>

    <a href="/contact"
       class="text-black hover:text-orange-500 transition">
      Contact
    </a>

    <a href="/about"
       class="text-black hover:text-orange-500 transition">
      About
    </a>

  </nav>
</div>



<script>
document.addEventListener("DOMContentLoaded", async () => {

  /* -----------------------------------
     ELEMENT REFERENCES
  ----------------------------------- */
  const menuBtn = document.getElementById("mobileMenuBtn");
  const mobileMenu = document.getElementById("mobileMenu");

  const profileBtn = document.getElementById("profileButton");
  const profileMenu = document.getElementById("profileMenu");

  const guestMenu = document.getElementById("guestMenu");
  const userMenu = document.getElementById("userMenu");
  const logoutBtn = document.getElementById("logoutButton");

  const avatar = document.getElementById("userAvatar");
  const nameEl = document.getElementById("userName");
  const icon = document.getElementById("defaultUserIcon");

  const wishlistBadge = document.getElementById("wishlistCount");
  const cartBadge = document.getElementById("cartCount");
  const profileImage = document.getElementById("userProfileImage");


  /* -----------------------------------
     MOBILE MENU
  ----------------------------------- */
 menuBtn.addEventListener("click", () => {

  if (mobileMenu.classList.contains("hidden")) {
    mobileMenu.classList.remove("hidden");

    setTimeout(() => {
      mobileMenu.classList.remove("opacity-0", "-translate-y-6", "scale-95");
      mobileMenu.classList.add("opacity-100", "translate-y-0", "scale-100");
    }, 5);
  } 
  else {
    mobileMenu.classList.remove("opacity-100", "translate-y-0", "scale-100");
    mobileMenu.classList.add("opacity-0", "-translate-y-6", "scale-95");

    setTimeout(() => {
      mobileMenu.classList.add("hidden");
    }, 300);
  }

});


  /* -----------------------------------
     PROFILE DROPDOWN MENU
  ----------------------------------- */
  if (profileBtn && profileMenu) {
    profileBtn.addEventListener("click", (e) => {
      e.stopPropagation();
      profileMenu.classList.toggle("hidden");
    });

    // Close on outside click
    document.addEventListener("click", (e) => {
      if (!profileMenu.contains(e.target) && !profileBtn.contains(e.target)) {
        profileMenu.classList.add("hidden");
      }
    });
  }


  /* -----------------------------------
     SESSION CHECK
  ----------------------------------- */
  try {
    const response = await fetch('/check-session');
    const data = await response.json();

    if (data.isLoggedIn) {
      guestMenu?.classList.add("hidden");
      userMenu?.classList.remove("hidden");

      if (avatar) {
        avatar.src = data.profilePic || "/images/default-avatar.png";
        avatar.classList.remove("hidden");
      }

      if (nameEl) {
        nameEl.textContent = data.name || "User";
        nameEl.classList.remove("hidden");
      }

      icon?.classList.add("hidden");

    } else {
      guestMenu?.classList.remove("hidden");
      userMenu?.classList.add("hidden");

      avatar?.classList.add("hidden");
      nameEl?.classList.add("hidden");
      icon?.classList.remove("hidden");
    }

  } catch (err) {
    console.error("Session check failed:", err);
  }


  /* -----------------------------------
     LOGOUT WITH CONFIRMATION
  ----------------------------------- */
  if (logoutBtn) {
    logoutBtn.addEventListener("click", async (e) => {
      e.preventDefault();

      Swal.fire({
        title: "Are you sure?",
        text: "Do you really want to logout?",
        icon: "warning",
        showCancelButton: true,
        confirmButtonText: "Yes, Logout",
        cancelButtonText: "Cancel",
        confirmButtonColor: "#d33",
        cancelButtonColor: "#3085d6"
      }).then(async (result) => {
        if (result.isConfirmed) {
          try {
            await fetch('/logout', { method: 'POST' });
            window.location.href = "/";
          } catch (error) {
            Swal.fire("Error", "Logout failed", "error");
          }
        }
      });
    });
  }


  /* -----------------------------------
     WISHLIST & CART COUNT
  ----------------------------------- */
  async function updateCounts() {
    try {
      const response = await fetch("/get-counts");
      const data = await response.json();

      if (wishlistBadge) {
        if (data.wishlistCount > 0) {
          wishlistBadge.textContent = data.wishlistCount;
          wishlistBadge.classList.remove("hidden");
        } else {
          wishlistBadge.classList.add("hidden");
        }
      }

      if (cartBadge) {
        if (data.cartCount > 0) {
          cartBadge.textContent = data.cartCount;
          cartBadge.classList.remove("hidden");
        } else {
          cartBadge.classList.add("hidden");
        }
      }

    } catch (err) {
      console.error("Error updating counts:", err);
    }
  }

  updateCounts();


  /* -----------------------------------
     USER PROFILE IMAGE
  ----------------------------------- */
  async function loadUserProfile() {
    try {
      const res = await fetch("/get-user-profile");
      const data = await res.json();

      if (profileImage) {
        profileImage.src = data.profileImage || "/images/pf.jpg";
      }

      if (!data.loggedIn) {
        nameEl?.classList.add("hidden");
      }

    } catch (err) {
      console.error("Profile load failed:", err);
    }
  }

  loadUserProfile();

});
</script>


</body>

</html>
