<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ELITECART</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</head>
<body class="bg-gray-100">
  <!-- Navbar -->
  <div
    class="fixed z-50 top-0 w-screen h-16 sm:h-20 flex items-center justify-between px-8 bg-black border-b-2 border-yellow-400 shadow-lg">

    <!-- Left: Logo/Brand -->
    <div class="flex items-center space-x-2">
      <span class="text-3xl sm:text-4xl font-bold font-serif text-white tracking-wide">Elite Cart</span>
    </div>

    <!-- Center: Nav Links -->
    <nav class="hidden md:flex items-center space-x-8 text-base sm:text-lg text-white font-medium">
      <a href="/" class="hover:text-yellow-400 transition">Home</a>
      <a href="/product" class="hover:text-yellow-400 transition">Shop</a>
      <a href="/contact" class="hover:text-yellow-400 transition">Contact us</a>
      <a href="/about" class="hover:text-yellow-400 transition">About</a>
    </nav>

    <!-- Right: Icons + Profile Dropdown -->
    <div class="flex items-center space-x-8">
      <!-- Icons -->
      <div class="hidden sm:flex items-center space-x-6 text-white text-xl">
        <a href="/wishlist" class="hover:text-yellow-400">
          <i class="fa-regular fa-heart"></i>
        </a>
        <a href="/cart" class="hover:text-yellow-400">
          <i class="fa-solid fa-cart-shopping"></i>
        </a>
      </div>

      <!-- Profile Dropdown -->
      <div class="relative">
        <button id="profileButton" class="hover:text-yellow-400 focus:outline-none text-white">
          <i class="fa-regular fa-user text-xl"></i>
        </button>

        <!-- Dropdown Menu -->
        <div id="profileMenu"
          class="hidden absolute right-0 mt-3 w-56 bg-white border border-gray-200 rounded-lg shadow-lg z-50">

          <!-- When not logged in -->
          <div id="guestMenu" class="p-4 border-b">
            <span class="text-sm text-gray-600">Welcome!</span>
            <div class="mt-2 flex gap-2">
              <a href="/login"
                class="text-sm font-semibold text-gray-900 bg-gray-100 px-3 py-1 rounded hover:bg-gray-200">Login</a>
              <a href="/signup"
                class="text-sm font-semibold text-white bg-yellow-600 px-3 py-1 rounded hover:bg-yellow-700">Sign
                Up</a>
            </div>
          </div>

          <!-- When logged in -->
          <ul id="userMenu" class="hidden py-2 text-sm text-gray-700">
            <li>
              <a href="/profile" class="flex items-center px-4 py-2 hover:bg-gray-100">
                <i class="fa-regular fa-user mr-2"></i> My Profile
              </a>
            </li>
            <li>
              <a href="/orders" class="flex items-center px-4 py-2 hover:bg-gray-100">
                <i class="fa-solid fa-box mr-2"></i> Orders
              </a>
            </li>
            <li>
              <a href="/wishlist" class="flex items-center px-4 py-2 hover:bg-gray-100">
                <i class="fa-regular fa-heart mr-2"></i> Wishlist
              </a>
            </li>
            <li>
              <button id="logoutButton"
                class="w-full text-left flex items-center px-4 py-2 hover:bg-gray-100">
                <i class="fa-solid fa-right-from-bracket mr-2"></i> Logout
              </button>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>


<script>
  const profileBtn = document.getElementById("profileButton");
  const profileMenu = document.getElementById("profileMenu");
  const guestMenu = document.getElementById("guestMenu");
  const userMenu = document.getElementById("userMenu");
  const logoutBtn = document.getElementById("logoutButton");

  // Toggle dropdown
  profileBtn.addEventListener("click", () => {
    profileMenu.classList.toggle("hidden");
  });

  // Close menu if clicked outside
  document.addEventListener("click", (e) => {
    if (!profileBtn.contains(e.target) && !profileMenu.contains(e.target)) {
      profileMenu.classList.add("hidden");
    }
  });

  // Session check
  document.addEventListener("DOMContentLoaded", async function () {
    try {
      const response = await fetch('/check-session');
      const data = await response.json();

      if (data.isLoggedIn) {
        guestMenu.classList.add("hidden");
        userMenu.classList.remove("hidden");
      } else {
        guestMenu.classList.remove("hidden");
        userMenu.classList.add("hidden");
      }
    } catch (error) {
      console.error("Session check error:", error);
    }
  });

  // Logout with confirmation
  if (logoutBtn) {
    logoutBtn.addEventListener("click", async (e) => {
      e.preventDefault();

      Swal.fire({
        title: "Are you sure?",
        text: "Do you really want to logout?",
        icon: "warning",
        showCancelButton: true,
        confirmButtonText: "Yes, Logout",
        cancelButtonText: "Cancel",
        confirmButtonColor: "#d33",
        cancelButtonColor: "#3085d6"
      }).then(async (result) => {
        if (result.isConfirmed) {
          try {
            await fetch('/logout', { method: 'POST' });
            window.location.href = "/"; 
          } catch (error) {
            console.error("Logout failed:", error);
            Swal.fire("Error", "Logout failed. Try again.", "error");
          }
        }
      });
    });
  }
</script>

</body>

</html>
